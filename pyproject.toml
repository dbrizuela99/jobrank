[project]
name = "jobrank"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "debrizt", email = "dbrizuela99@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.28.1",
]

[project.scripts]
jobrank = "jobrank:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
